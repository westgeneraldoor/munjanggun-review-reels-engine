# _audit_2026-06-09.md — 문장군 콘텐츠 엔진 종합 감사

> 감사 모드: Mode A — 전수 스캔
> 대상: 문장군 리뷰 기반 숏폼 콘텐츠 엔진
> 범위: 프로젝트 취지, 문서, 코드, 산출물, 운영성, 유지보수성, 안정성

---

## 감사 결론

프로젝트 방향은 유지되고 있다.
핵심 철학인 "리뷰를 광고가 아니라 사연으로 만든다"는 문서와 생성 구조에 남아 있고, 리뷰번호/상품주문번호 기반 운영 추적도 도입되었다.

다만 현재 리스크는 기능 부재가 아니라 운영 드리프트다.
프롬프트 품질 규칙, 검증 코드, 현황판, PRD가 완전히 같은 말을 하지 않는다.

감사 후 보강 완료:

- HOOK 공감 질문형 광고 문장 FAIL 검증 추가
- `.gitignore` 추가
- `PROJECT_DASHBOARD.md` 점수 드리프트 지표 추가
- `PRD_v1.0.md` output 구조 갱신

---

## 심각

현재 즉시 실패로 보이는 항목은 없다.
`generate.py` 기본 검증, SRT 생성, TTS 생성, 메타데이터 삽입은 테스트 기준 통과했다.

검증:

```text
python -m unittest tests.test_validate_script
21 tests OK

python -m py_compile generate.py tests/test_validate_script.py
OK
```

---

## 경고

### 1. 프롬프트 품질 규칙과 검증기가 불일치한다 — 감사 후 보강 완료

근거:

- `prompts/screenplay.txt:51` — `"~하시죠?" 같은 공감 질문으로 시작하지 마세요.`
- `prompts/screenplay.txt:57-59` — 나쁜 HOOK 예시에 공감 질문 금지 명시
- 감사 당시 `validate_script()`는 포맷/메타데이터/상품명/금지어 중심 검증이었다.
- 감사 당시 `output/inbox_20260609/010_구축소음_20260609_095709/010_구축소음_script.md:17`의 `"현관 소음 때문에 고통받으셨죠?"`가 통과되었다.
- 후속 조치로 `validate_script()`에 `하시죠?`, `고민이셨나요?`, `불편하셨나요?`, `스트레스 받으셨나요?`, `고통받으셨죠?` 패턴 FAIL 검증을 추가했다.

위험:

콘텐츠 품질이 프롬프트 운에 맡겨진다.
사장님이 싫어했던 공감 질문형 HOOK이 다시 살아날 수 있다.

해결 방향:

`validate_script()`에 HOOK caption/narration의 질문형 시작 패턴을 `[WARN]` 또는 `[FAIL]`로 검사한다.

### 2. PRD 출력 구조가 최신 운영 구조와 어긋나 있다 — 감사 후 보강 완료

근거:

- `PROJECT_TASKS.md:65` — 최신 구조는 `output/{리뷰묶음}/001_짧은라벨_...`
- `_context.md:8` — 최신 구조는 묶음별 output
- `PROJECT_DASHBOARD.md:17` — 최신 구조도 묶음별 output
- 감사 당시 `PRD_v1.0.md`는 묶음 없는 output 예시를 일부 유지하고 있었다.
- 후속 조치로 F-004/output 구조를 `output/{리뷰묶음}/번호_짧은라벨_타임스탬프/` 기준으로 갱신했다.

위험:

새 작업자가 PRD만 보고 output 루트 직접 저장 방식으로 되돌릴 수 있다.

해결 방향:

PRD의 F-004/output 구조/EVAL 항목을 `output/{리뷰묶음}/...` 기준으로 갱신한다.

### 3. 대시보드가 수동 문서라 빠르게 낡을 가능성이 크다

근거:

- `PROJECT_DASHBOARD.md:13` — 진행률 수동 기재
- `PROJECT_DASHBOARD.md:46-48` — 리뷰 수 수동 기재
- `PROJECT_DASHBOARD.md:52-57` — 패키지 수와 음원 길이 수동 기재
- `PROJECT_DASHBOARD.md:87-110` — 정식 등록 리뷰 점수 수동 기재

위험:

패키지가 몇 개 더 생성되면 현황판 숫자와 실제 파일 수가 갈라진다.

해결 방향:

초기에는 수동 유지가 가능하지만, 패키지 10개 이상부터 `tools/update_dashboard.py` 같은 자동 집계 스크립트를 만든다.

### 4. 프로젝트 위생 체계가 약하다 — 감사 후 부분 보강 완료

근거:

- `.git` 없음
- 감사 당시 `.gitignore` 없음
- `.env` 존재
- `.codex_deps/`, `__pycache__/`, `output/`이 프로젝트 루트에 존재

위험:

나중에 git을 만들거나 파일을 옮길 때 API 키, 의존성 폴더, 생성물이 같이 섞일 수 있다.

해결 방향:

후속 조치로 최소 `.gitignore`를 추가했다.
ignore 항목: `.env`, `.codex_deps/`, `__pycache__/`, `tests/__pycache__/`, `output/`, `*.pyc`.

### 5. 재생성 시 기존 패키지를 삭제한다

근거:

- `generate.py:369` — `save_script()`
- `generate.py:389` — 같은 source marker를 찾으면 `shutil.rmtree(existing)`로 기존 output 폴더 삭제

위험:

TTS 품질 비교, 이전 버전 복원, 사장님 선택 전후 비교가 어렵다.

해결 방향:

운영 모드는 기존 삭제 유지 가능.
비교가 필요한 시점에는 `--keep-history` 또는 `--no-clean` 옵션을 추가한다.

---

## 참고

### 1. 핵심 목적은 유지되고 있다

근거:

- `GEMINI.md` 상단 철학 유지
- `PROJECT_DASHBOARD.md`에 리뷰 자산/후보/생성 패키지 연결
- `REVIEW_CONTENT_COMMAND.md`가 후보 제안 후 사용자 선택을 강제
- script.md front matter에 `review_number`, `product_order_number`가 들어감

판정:

프로젝트가 "리뷰 텍스트 → 숏폼 콘텐츠 패키지"라는 범위를 아직 벗어나지 않았다.

### 2. 콘텐츠 품질은 올라갔지만 자동 판단은 아직 약하다

근거:

- `PROJECT_DASHBOARD.md:87-110` — 후보 점수는 있으나 사람이 정한 초기 점수
- `REVIEW_CONTENT_COMMAND.md:51-59` — 후보 평가 기준은 문서화되어 있으나 코드화되지 않음

판정:

지금은 총괄 판단이 있어야 한다.
완전 자동 후보 선별은 아직 이르다.

### 3. TTS 정책은 현실적으로 정리됐다

근거:

- `_context.md:10-11` — 속도 고정, 글자수/음원 길이는 품질 우선
- `generate.py:656-700` — 글자수 기반 목표 길이 계산 후 속도 조정

판정:

이전의 "35초에 무조건 맞추기"보다 낫다.
다만 SRT가 35초 고정이라 긴 음원과 자막 길이 차이는 수동 편집에서 확인해야 한다.

---

## 위생 점검

| 항목 | 상태 | 조치 |
|------|------|------|
| 테스트 | 통과 | 유지 |
| py_compile | 통과 | 유지 |
| .git | 없음 | 도입 여부 결정 필요 |
| .gitignore | 추가됨 | 유지 |
| .env | 존재 | gitignore 필수 |
| output | 루트에 존재 | gitignore 또는 운영 폴더로 명확히 분리 |
| .codex_deps | 루트에 존재 | gitignore 필수 |
| __pycache__ | 존재 | gitignore 필수 |

---

## 우선순위 조치안

1. `validate_script()`에 HOOK 공감 질문형/광고형 패턴 검증 추가 — 완료
2. `PRD_v1.0.md` output 구조를 묶음별 output 기준으로 갱신 — 완료
3. `.gitignore` 추가 — 완료
4. `PROJECT_DASHBOARD.md` 갱신 규칙 또는 자동 집계 스크립트 추가 — 부분 완료: 점수 드리프트 지표 추가, 자동화는 패키지 10개 이후 검토
5. 재생성 히스토리 보존 옵션 검토

---

## 감사자가 본 현재 판정

프로젝트는 MVP 제작 단계로는 충분히 작동한다.
운영 단계로 넘어가려면 "문서-코드-산출물-현황판"의 동기화 체계를 더 단단히 해야 한다.

가장 먼저 고칠 것은 기능 추가가 아니라 HOOK 품질 검증과 PRD 드리프트다.
