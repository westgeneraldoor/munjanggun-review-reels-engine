# CONTENT_QUALITY_STANDARD.md — 콘텐츠 패키지 품질 기준

이 문서는 문장군 리뷰 콘텐츠 엔진의 최종 품질 기준이다.
목표는 기준 충족이 아니라, 실제 인스타그램에 올렸을 때 저장/공유/문의 가능성이 있는 패키지를 만드는 것이다.

## 기본 원칙

- 리뷰를 광고로 만들지 않는다. 사건과 생활 변화로 만든다.
- 좋은 리뷰를 짧게 요약하는 것이 아니라, 시청자가 저장할 이유를 만든다.
- 최종 산출물은 `script.md`, `subtitle.srt`, `voice.mp3`가 서로 같은 방향을 말해야 한다.
- `script.md`에는 캡션과 해시태그까지 포함되어야 한다.

## script.md 품질

- HOOK은 공감 질문이 아니라 사건이어야 한다.
- HOOK은 추상 결론이 아니라 구체 상황이어야 한다. `한 달 뒤 진짜입니다`, `좋아졌습니다`, `만족했습니다`처럼 대상이 빠진 문장은 실패다.
- 첫 화면용 HOOK은 최소한 `무엇이/누구에게/어떻게 달라졌는지` 중 2개 이상을 포함해야 한다.
- 첫 3초 안에 상품명이 아니라 상황이 보여야 한다.
- 리뷰 원문에 있는 구체 디테일을 살린다.
- 원문에 없는 고객 반응, 효과, 수치, 가격은 만들지 않는다.
- CLOSE는 실제 리뷰 문장 또는 리뷰의 감정을 가져와야 한다.

## subtitle.srt 품질

- SRT는 자막 텍스트 기준이다.
- 내레이션 전문을 그대로 넣지 않는다.
- 화면에서 읽기 쉬운 짧은 문장이어야 한다.
- 자막에 `[자막 텍스트]` 같은 placeholder가 남으면 실패다.

## 이미지/생성 인서트 품질

- 실제 현장 사진이 최우선이다.
- 사진이 많아도 음성/자막의 핵심 의미를 보여주지 못하면 그대로 쓰지 않는다.
- 생성 이미지는 실제 시공 증거가 아니라 이해 보조 인서트로만 사용한다.
- 생성 이미지는 생활상황, 불편 체감, 동선, 보이지 않는 문제 설명에 한정한다.
- 실제 시공 완료, 제품 마감, 유리 색상, 레일, 댐퍼, 실측, 리뷰 캡처는 생성 이미지로 대체하지 않는다.
- 생성 이미지를 쓰면 recipe에 `generated_asset`, `generated_reason`, `not_real_proof`, `visual_claim`, `literal_qa_result`를 남긴다.
- 생성 컷은 나레이션의 구체 문장을 문자 그대로 만족해야 한다. `문턱 없이`라고 하면 문턱, 턱, 레일, 단차가 보이면 실패다.
- 생성 컷이 광고 이미지처럼 떠 보이거나 실제 현장 사진과 이질감이 크면 짧은 인서트로만 쓰거나 폐기한다.

## voice.mp3 품질

- TTS는 내레이션 기준이다.
- 리뷰 콘텐츠는 여자 진행자 톤을 기본으로 한다.
- 말 속도는 느릿한 낭독이 아니라 숏폼 리듬이어야 한다.
- 원본 TTS를 최종 길이에 맞추기 위해 과하게 압축하면 실패다. 원본 길이/최종 길이 압축률이 `1.20` 이상이면 재생성하고, `1.25` 이상이면 렌더 금지다.
- 음원 길이에 맞추려고 내용을 삭제하지 않는다.
- 기존 문장군 승인 음성과 다르게 지나치게 차분하거나 낮고 처지는 톤이면 실패다.
- 받침, 조사, 문장 끝 발음이 뭉개지면 수치가 통과해도 실패다.
- 홈쇼핑, 뉴스 앵커, ARS, 유튜브 AI 낭독 느낌은 실패다.

## 캡션 품질

- 캡션은 기준 충족용 요약문이 아니다.
- 저장/공유/문의 전환을 노리는 게시글 본문이다.
- 최소 5줄 이상, 공백 제외 180자 이상을 기본으로 한다.
- 원문 리뷰의 구체 디테일을 최소 2개 이상 포함한다.
- 저장 이유와 자연스러운 CTA가 있어야 한다.
- 자세한 기준은 `POSTING_COPY_STANDARD.md`를 따른다.

## 해시태그 품질

- 20~25개를 기본 목표로 한다.
- 필수 태그: `#문장군`, `#문장군중문`, `#문장군시공`.
- 주제별 태그는 `문장군 인스타그램/INSTAGRAM_HASHTAG_BANK.md`를 참조한다.
- 스팸성 태그와 경쟁사 태그는 금지한다.

## 최종 검수

완료 보고 전 아래를 확인한다.

```text
script.md 품질 검증 통과
subtitle.srt 생성 및 자막 기준 확인
voice.mp3 생성 및 파일 크기 0 초과
voice.mp3 원본 TTS 길이/최종 길이/압축률 검수
voice.mp3 톤과 발음이 기존 승인 음성과 다르지 않은지 확인
생성 이미지 사용 시 실제 증거가 아닌 인서트로 표기
생성 이미지 literal QA 통과
caption/hashtag가 script.md 내부에 있음
caption.txt / hashtag.txt 별도 파일 없음
PROJECT_DASHBOARD.md 갱신
_context.md 갱신
```
